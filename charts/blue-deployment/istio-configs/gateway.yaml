apiVersion: networking.istio.io/v1beta1
kind: Gateway
metadata:
  name: ssi-dim-wallet-gateway
spec:
  selector:
    istio: ingressgateway
  servers:
    - port:
        number: 80
        name: http
        protocol: HTTP
      hosts:
        - "ssi-dim-wallet-stub-demo.arena2036-x.de"
