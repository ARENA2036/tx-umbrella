apiVersion: v1
kind: Service
metadata:
  name: istio-ingressgateway
  namespace: istio-system
  annotations:
    external-dns.alpha.kubernetes.io/hostname: ssi-dim-wallet-stub-demo.arena2036-x.de
spec:
  type: LoadBalancer
  ports:
    - name: http
      port: 80
  selector:
    istio: ingressgateway
