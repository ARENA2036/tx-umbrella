# #############################################################################
# Copyright (c) 2023,2024 Contributors to the Eclipse Foundation
# Copyright (c) 2026 ARENA2036 e.V.
#
# See the NOTICE file(s) distributed with this work for additional
# information regarding copyright ownership.
#
# This program and the accompanying materials are made available under the
# terms of the Apache License, Version 2.0 which is available at
# https://www.apache.org/licenses/LICENSE-2.0.
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS, WITHOUT
# WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied. See the
# License for the specific language governing permissions and limitations
# under the License.
#
# SPDX-License-Identifier: Apache-2.0
# #############################################################################
---
name: Install Portal (Install or Upgrade)

on:
  push:
    branches:
      - feat/install-portal-script
      - develop
      - main

  # pull_request:
  #   paths:
  #     - 'charts/**'
  #     - '!**/README.md'
  #     - '.github/workflows/helm-checks.yaml'
    
  workflow_dispatch:
    inputs:
      cluster_context:
        default: 'k8s-ap6-01'
        required: false
        description: "Select the environment"
        type: choice
        options:
        - k8s-ap6-01
        - staging
        - prod
      release_name:
        description: 'helm deployment name e.g, portal-dev'
        default: 'portal-dev'
        required: false
        type: string
      release_type:
        default: 'upgrade'
        required: false
        description: "Select the command"
        type: choice
        options:
        - install
        - upgrade
        - uninstall
      helm_values_file:
        description: 'the relative path to the helm values.yaml'
        default: 'values-adopter-portal-dev.yaml'
        required: false
        type: string
      namespace:
        description: 'the namespace where the portal is to be deployed'
        default: 'portal'
        required: false
        type: string

jobs:
  install-portal:
    runs-on: ubuntu-latest
    steps:
      - name: Import Secrets from staging Vault
        id: import-staging-secrets
        # retrieve values from vault for staging
        if: ${{ github.event.inputs.cluster_context  == 'staging' }}
        uses: hashicorp/vault-action@v2
        with:
          url: ${{ secrets.VAULT_ADDR_STAGING }}
          token: ${{ secrets.VAULT_TOKEN_STAGING }}
          secrets: |
            staging/data/infra/cluster-staging config | KUBECONFIG ;

      - name: Import Secrets from prod Vault
        id: import-prod-secrets
        # retrieve values from vault for prod
        if: ${{ github.event.inputs.cluster_context == 'prod' }}
        uses: hashicorp/vault-action@v2
        with:
          url: ${{ secrets.VAULT_ADDR_PROD }}
          token: ${{ secrets.VAULT_TOKEN_PROD }}
          secrets: |
            staging/data/infra/cluster-prod config | KUBECONFIG ;

      - name: Import Secrets from dev Vault
        id: import-dev-secrets
        # retrieve values from vault for prod
        if: ${{ github.event.inputs.cluster_context == 'k8s-ap6-01' }}
        uses: hashicorp/vault-action@v2
        with:
          url: ${{ secrets.VAULT_ADDR_DEV }}
          token: ${{ secrets.VAULT_TOKEN_DEV }}
          secrets: |
            staging/data/infra/cluster-dev config | KUBECONFIG ;

      - name: Install kubectl
        run: |
          echo "kubectl is not installed, installing Kubectl..."
          curl -LO -s "https://dl.k8s.io/release/$(curl -L -s https://dl.k8s.io/release/stable.txt)/bin/linux/amd64/kubectl"
          # Make it executable
          chmod +x kubectl
          # Move to a directory in your PATH
          sudo mv kubectl /usr/local/bin/
          # Verify installation
          kubectl version --client
          echo "kubectl installed successfully!"
          ## kubectl version check
          echo "Checking kubectl version..."
          echo `kubectl version`
      
      
      - name: Setup Cluster Context
        run: |
          mkdir -p $HOME/.kube
          CONFIG_PATH="$HOME/.kube/config"
          if [[ "${{ github.event.inputs.cluster_context }}" == "staging" ]]; then  
            echo "Setting context for staging..."  
            cat <<EOF > $CONFIG_PATH
          ${{ steps.import-staging-secrets.outputs.KUBECONFIG }}
          EOF
          elif [[ "${{ github.event.inputs.cluster_context }}" == "prod" ]]; then
            echo "Setting context for production..."  
            cat <<EOF > $CONFIG_PATH
          ${{ steps.import-prod-secrets.outputs.KUBECONFIG }}
          EOF
          elif [[ "${{ github.event.inputs.cluster_context }}" == "k8s-ap6-01" ]]; then
            echo "Setting context for development..."  
            cat <<EOF > $CONFIG_PATH
          ${{ steps.import-dev-secrets.outputs.KUBECONFIG }}
          EOF
          else
            echo "Not a valid environment"
            exit 1
          fi
          chmod 600 $CONFIG_PATH
          export KUBECONFIG="$CONFIG_PATH"
          echo "KUBECONFIG=$CONFIG_PATH" >> $GITHUB_ENV
          kubectl --context=${{ github.event.inputs.cluster_context }} cluster-info
 

      - name: Set up Helm
        uses: azure/setup-helm@b7246b12e77f7134dc2d460a3d5bad15bbe29390 # v4.1.0
        with:
          version: v3.12.1
      
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
      
      - name: Deploy Portal
        working-directory: hack/
        run: |
          chmod +x ./install-release.sh
          echo "Running deployment script..."
          if [ "${{ github.event.inputs.release_type }}" = "uninstall" ]; then
            helm uninstall ${{ github.event.inputs.release_name }} --namespace ${{ github.event.inputs.namespace }}
          elif [ "${{ github.event.inputs.release_type }}" = "install" ] || [ "${{ github.event.inputs.release_type }}" = "upgrade" ]; then
            ./install-release.sh ${{ github.event.inputs.cluster_context }} ${{ github.event.inputs.release_name }} ${{ github.event.inputs.release_type }} ${{ github.event.inputs.helm_values_file }} ${{ github.event.inputs.namespace }}
          else
            echo "Invalid release_type"
            exit 1
          fi
